<br><br><br><br>
<div class="ui centered raised card" style="width: 500px;">
<br><br>
 <h2 class="ui center aligned icon header">
  <i class="circular location icon"> </i>
 </h2>
 <br><br>
  <div class="content">
    <div class="header" style="font-size: 2em;"><%= "#{@trip.name}" %></div>
    </div>
  <div class="ui placeholder segment" style="
    width: 500px;
    /* align-content: center;
   ">
  <div class="ui two column stackable center aligned grid">
    <div class="ui vertical divider">Or</div>
    <div class="middle aligned row">
      <div class="column">
        <div class="ui icon header">
          <i class="search icon"></i>
          <!-- write a method that pulls in the name of your location-->
        </div>
        <div class="field">
          <div class="ui search">
            <div class="ui icon input">
              <input class="prompt" type="text" placeholder="Search locations...">
              <i class="search icon"></i>
            </div>
            <div class="results"></div>
          </div>
        </div>
      </div>
      <div class="column">
        <div class="ui icon header">
          <i class="circular list icon"></i>
          Add New location
        </div>
          <%= link_to 'Create location', new_trip_location_path(@trip), class: "ui orange button inverted" %>
      </div>
    </div>
  </div>
  </div>
    <div class="extra content" style="margin-right: 20px; margin-bottom: -5px;">
        <p class="ui content center aligned"> Scroll Down for locations </p>
        <p class="ui content center aligned" style="margin-bottom: -5px;"> ↓ </p>
    </div>
    <div class="ui segments" style="padding: 5px;">
        <% @trip.locations.each do |location|%>
        <br>
        <div class="ui segments" style="padding: 30px; margin-top: -10px; margin-bottom: -10px;">
          <div class="ui-segment">
            <h2>
              <%= link_to location.name, location_path(location.id) %> 
            </h2>
            <!-- call method to sort by priorety-->
            <div class="ui mini buttons" style="margin-left: -10px;">
              <button class="ui inverted button red">
                <%= link_to 'Delete', trip_location_path(@trip, location.id), method: :delete %>
              </button>
              <div class="or"></div>
              <button class="ui inverted button blue">
                <%= link_to 'Edit', edit_trip_location_path(@trip, location.id) %>
              </button>
            </div>
            <br>
          </div>
      </div>
      <br>
      <%end%>
    </div>
    <div class="extra content">
        <%= link_to 'Back',  trips_path, class: "ui secondary button inverted"%>
    </div>
</div>
</div>